// swift-tools-version:5.0
import PackageDescription

let package = Package(
    name: "StandardForm",
    products: [
        .library(
            name: "StandardForm",
            targets: ["StandardForm"])
    ],
    targets: [
        .target(
            name: "StandardForm",
            dependencies: [])
    ]
)
